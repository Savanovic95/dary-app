version: "3.8"

services:
    database:
        image: mysql:8.0
        ports:
            - 3306:3306
        enviroment:
            - MYSQL_DATABASE=${DB_DATABASE}
            - MYSQL_DATABASE=${DB_DATABASE}
            - MYSQL_DATABASE=${DB_DATABASE}
            - MYSQL_DATABASE=${DB_DATABASE}

volumes:
    db-data:
